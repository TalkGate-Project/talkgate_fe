# 회생·파산(Analysis) API 연동 최종 점검 + 브라우저 QA 체크리스트

작성일: 2026-07-21. 목적: 최신 Swagger(Analysis / Analysis Bulk Action / Analysis Fee / Analysis Fee
Statistics / Analysis Partner)와 현재 코드를 대조해 연동 현황을 확정하고, 배포 전 브라우저에서 직접
눌러봐야 할 플로우를 체크리스트로 정리한다. 데스크톱 우선(§2) → 모바일은 별도 트랙(§3)으로 뺐다.

---

## 1. API 연동 현황

### Analysis

| 엔드포인트 | 연동 상태 | 사용처 |
|---|---|---|
| `POST /v1/analysis` | ✅ | `DebtReliefService.createDiagnosis` → 새 진단 폼 제출 |
| `GET /v1/analysis` | ✅ | `DebtReliefService` 목록 조회 → 허브 테이블/카드 |
| `GET /v1/analysis/{id}` | ✅ | `DebtReliefService.getDiagnosisDetail` → 상세 페이지 전체 |
| `PATCH /v1/analysis/{id}` | ✅ (이번 세션에 상태 게이팅 수정) | `DebtReliefService.updateProcedureProgress` → 절차 전환 + 현재 단계 설정 |
| `DELETE /v1/analysis/{id}` | ✅ | `ResultDeleteButton.tsx` |
| `POST /v1/analysis/{id}/accept` | ✅ | `AnalysisReviewBanner.tsx` (변호사) |
| `GET /v1/analysis/{id}/chat` | ✅ | `useDebtReliefAiChat.ts` |
| `POST /v1/analysis/{id}/chat/stream` | ✅ | `useDebtReliefAiChat.ts` (SSE) |
| `GET /v1/analysis/{id}/connectable-customers` | ✅ | `CustomerMatchModal.tsx` |
| `PATCH /v1/analysis/{id}/customer` | ✅ | `CustomerMatchModal.tsx`, `CustomerCreateMatchModal.tsx` |
| `DELETE /v1/analysis/{id}/customer` | ✅ | `ResultHeader.tsx`(연결 해제) |
| `POST /v1/analysis/{id}/deliver` | ⚠️ 선언만, 미사용 | 단건 공유도 `bulkDeliver`로 통일 처리 — 문제 아님(의도적 단순화) |
| `GET /v1/analysis/{id}/deliveries` | ✅ (제한적) | `ResultDeleteButton.tsx`가 삭제 전 활성 공유 여부 체크용으로만 사용 |
| `PATCH /v1/analysis/{id}/input` | ✅ | `DebtReliefService.updateDiagnosis`(재분석) |
| `GET /v1/analysis/{id}/procedure-changes` | ❌ 미사용 | 노출 화면 없음. 상세의 `procedureStepHistory`로 대체되는 듯 보이나 확인 필요 |
| `POST /v1/analysis/{id}/reject` | ✅ | `AnalysisReviewBanner.tsx` (변호사) |
| `POST /v1/analysis/{id}/send-sms` | ✅ | `SectionSmsSend.tsx`, `SectionProcedureGuide.tsx`(단계별 문자) |
| `GET /v1/analysis/procedures` | ❌ 미사용 | 절차 마스터를 클라이언트 하드코딩 값으로 사용 중(실질 문제 아님, 서버값과 다이버전 리스크만 존재) |
| `GET /v1/analysis/summary` | ✅ | 허브 요약 카드 |

### Analysis Bulk Action

| 엔드포인트 | 상태 | 사용처 |
|---|---|---|
| `POST /v1/analysis/bulk-delete` | ✅ | `DebtReliefHubContent.tsx` 일괄삭제 |
| `POST /v1/analysis/bulk-deliver` | ✅ | `AnalysisShareModal.tsx` (단건/일괄 공유 겸용) |

### Analysis Fee / Fee Statistics

| 엔드포인트 | 상태 | 사용처 |
|---|---|---|
| `POST /v1/analysis/{id}/fee-plan` | ✅ | `FeePaymentInfoModal.tsx` |
| `PATCH /v1/analysis/{id}/fee-plan` | ✅ | `FeePaymentInfoModal.tsx` |
| `POST .../installments/{id}/pay` | ✅ | `FeePaymentInfoModal.tsx` |
| `DELETE .../installments/{id}/pay` | ✅ | `FeePaymentInfoModal.tsx`(납부 취소) |
| `POST .../fee-plan/refund` | ✅ | `FeePaymentInfoModal.tsx` (전달사항 입력 포함) |
| `POST .../fee-plan/stop` | ✅ | `FeePaymentInfoModal.tsx` (전달사항 입력 포함) |
| `GET /v1/analysis/fee-statistics/summary` | ✅ | `FeePaymentStatusPanel.tsx` |
| `GET /v1/analysis/fee-statistics/installments` | ✅ | `FeePaymentStatusPanel.tsx` |

### Analysis Partner

| 엔드포인트 | 상태 |
|---|---|
| `GET /v1/analysis-partners` | ✅ 승인된 목록만 조회(`AnalysisShareModal.tsx`) |
| `POST` / `DELETE /{id}` / `PATCH /{id}/status` / `GET /requests` | 의도적으로 이 프론트엔드 범위 밖 — 별도 외부 admin이 전담 (`services/analysisPartners.ts` 상단 주석 참고) |

---

## 2. 이번 점검에서 새로 발견한 이슈

바로 고칠지는 별도 판단 필요 — 우선 목록만 남긴다.

1. ~~공유 철회로 이어지지 않는 삭제 안내 (막다른 길)~~ — **수정 완료(2026-07-22)**. 영업점이 공유 중인
   진단을 삭제하려 하면 `ResultDeleteButton.tsx`가 "공유를 철회한 뒤 다시 시도해주세요"라고 안내했는데,
   실제로 공유를 철회하는 버튼/화면이 어디에도 없어(`AnalysisService.revokeDelivery` 여전히 미사용)
   실행 불가능한 다음 행동을 지시하는 막다른 길이었음. 실제 철회 기능을 새로 만드는 방향도 검토했으나
   (서비스 메서드는 이미 구현되어 있어 작업량 자체는 크지 않았음) 사용자가 "일단 문구만 수정"으로
   범위를 좁히기로 결정 — 안내를 "공유받은 프로젝트와의 연결이 유지되는 동안은 삭제할 수 없습니다"로
   교체해 실행 불가능한 지시만 제거함. 철회 기능 자체는 여전히 없음(필요시 별도 작업으로 재검토).
2. ~~공유 시 참고사항(`referenceNote`) 노출 여부 불확실~~ — **원인 확정 후 수정 완료(이번 세션)**.
   `POST /v1/analysis/{id}/deliver`·`/bulk-deliver` 실 스펙 확인 결과 참고사항 필드명은
   `referenceNote`가 아니라 `message`였음. `AnalysisShareModal.tsx`가 `referenceNote`로 보내고 있어
   백엔드가 인식 못 하고 버렸을 가능성이 높음 — `DeliverAnalysisInput`/`BulkDeliverAnalysisItem`
   타입과 전송 지점을 `message`로 수정. `DiagnosisDetail.referenceNote`(읽는 쪽)는 실제 상세 응답
   스키마에 아예 없는 필드라 항상 null이었던 것도 확인됨 — 참고사항은 `messages` 타임라인의 "공유"
   항목으로만 확인 가능(`SectionDeliveryMessages`). §2-D-6에서 실제로 참고사항 입력 후 타임라인에
   찍히는지 확인 필요(수정 후 최초 검증).
3. ~~모바일 카드 리스트엔 공유 아이콘이 아예 없음~~ — **수정 완료(2026-07-22)**. `DiagnosisMobileCardList.tsx`
   영업점이 모바일에서 진단을 공유할 방법이 없었음 — 데스크톱 `DiagnosisTable`과 동일한
   `AnalysisShareIcon`/`handleShareItem`을 재사용해 카드 헤더 행(절차명 옆)에 공유 버튼 추가.
   카드 전체가 클릭 시 상세로 이동하는 구조라 `stopPropagation` 처리, 터치 영역은 `-m-2 p-2`로
   확장. 강제 CSS 오버라이드로 모바일 카드 레이아웃을 렌더링시켜 아이콘이 절차 배지 옆에 정상
   배치되는 것과, 버튼을 프로그래밍적으로 클릭해 공유 모달이 정상 열리고(재공유 흐름 진입 확인)
   카드 자체의 상세 이동은 트리거되지 않는 것까지 확인함(실제 390px 뷰포트는 이번에도 자동화로
   재현 불가해 이 방식으로 대체).
4. ~~"다시 분석하기"에 확인 모달 없음~~ — **수정 완료(2026-07-22)**. `DiagnosisFormContent.tsx`
   `handleAnalyze`. 재분석 성공 시 상태/절차/현재단계가 초기화되고 AI 채팅 이력이 삭제되는 되돌릴 수
   없는 부수효과가 있었는데 클릭 전 경고가 없었음 — 수정 모드(`isEdit`)에서만 `showConfirmModal`로
   확인을 받도록 추가(신규 생성은 잃을 기존 상태가 없어 대상 아님). 상담중/반려된 실 데이터가 없어
   실클릭 검증은 못 했고, 절차 변경 확인 모달과 동일한 패턴 재사용 + tsc/eslint 통과로 갈음.
5. ~~`/debt-relief/[id]/edit` 라우트에 상태 가드 없음~~ (2026-07-22 모바일 반응형 실브라우저 QA 중 발견,
   **수정 완료**) — `ResultHeader.tsx:385` `handleEdit`은 상담중/반려됨이 아니면 클릭 시 "정보수정 불가"
   안내 모달로 막았지만, 이 가드는 진입 버튼(`정보수정` 클릭)에만 걸려 있어 `/debt-relief/{id}/edit`
   라우트에 URL로 직접 들어가면 우회됐음. `getDiagnosisForm`(`debtRelief.ts`)이 이미 불러오는 상세
   응답에서 `status`를 함께 반환하도록 확장하고, `DiagnosisFormContent`의 편집 모드 로딩 effect에서
   상담중/반려됨이 아니면 동일한 안내 모달을 띄우고 상세 페이지로 즉시 리다이렉트하도록 추가(폼은
   채우지 않고 로딩 상태 유지 — 빈 폼이 잠깐이라도 보이지 않게 처리). 절차진행중 건으로 직접 URL
   접근해 실제로 차단·리다이렉트되는 것까지 브라우저로 확인함.
6. ~~변호사 수락 시 결제정보 있으면 계약대기중을 건너뛰는 분기~~ — **확인 완료, 조치 불필요(2026-07-23)**.
   영업점이 공유 이전에 이미 결제(수임료) 정보를 입력해둔 건을 변호사가 수락하면, 계약대기중을
   건너뛰고 바로 절차진행중으로 전환됨. 사용자가 최근 별도로 이미 확인한 사항이고 백엔드에도 공유
   완료 — 의도된 동작으로 확정, 프론트 대응 불필요.

---

## 3. 브라우저 QA 체크리스트 (데스크톱)

**사전 준비**: 영업점(analysis) 프로젝트 1개 + 변호사(lawyer) 프로젝트 1개, 두 프로젝트가 파트너로 연결된
테스트 계정 필요(`useProjectType`이 서브도메인 선택 프로젝트의 `type`으로 역할을 가른다).

### A. 영업점(analysis) 프로젝트

- [x] **새 진단 생성**: `/debt-relief/new` 5단계 폼 입력 → AI 진단 실행 → 결과 상세로 이동 (2026-07-23 브라우저 QA로 확인, 신규 테스트 진단 생성 성공)
- [ ] **목록/필터**: `/debt-relief` 검색·절차 필터·상태 필터·정렬(상담일)·페이지네이션·표시개수 변경 (미확인)
- [ ] **AI 채팅**: 상세 페이지에서 AI와 채팅 스트리밍 응답 확인 (미확인)
- [ ] **고객 매칭**: 미매칭 진단에 기존 고객 연결 / 신규 고객 생성 후 연결 / 연결 해제 (미확인)
- [x] **절차 전환(개인회생 ↔ 채무조정 ↔ 파산)** — `절차진행중` 상태에서 확인 완료
  - [ ] `계약대기중` 상태에서 셀렉트가 눌리는지는 별도로 재확인 못 함(테스트 중 계약대기중 케이스가 곧바로 절차진행중으로 넘어가 버림) — `상담중` 상태에서는 배지가 비활성(정적 텍스트)임은 확인
  - [x] 절차 전환 시 "추적 절차를 변경할까요? 1단계로 초기화됩니다" 확인 모달이 뜨고, 확정 후 실제로 1단계로 초기화됨(진행률 40%→13%로 리셋 확인)
- [x] **현재 단계로 설정** — `절차진행중` 상태에서 확인 완료
  - [ ] `계약대기중` 상태에서 버튼이 비활성화되는지는 이번 세션에 별도 재확인 못 함
  - [x] `절차진행중` 상태에서는 버튼이 활성화되고 정상 저장됨(2단계로 정상 이동, 담당자·시각 기록됨)
  - [x] 현재 단계보다 이전 단계(1단계)는 버튼이 비활성(회색)으로 표시됨
- [ ] **단계별 문자 발송**: UI 진입까지만 확인, **실제 발송 버튼은 안전상 누르지 않음**(연락처 있는 테스트 고객 010-0000-0000으로 실발송 리스크 있어 보류)
- [ ] **하단 "고객 문자 전송" 섹션**: 연락처 없으면 4개 버튼 모두 비활성 + 안내문구 표시되는 것만 확인(정상). 실제 발송은 미시도
- [x] **공유(단건)**: 목록의 공유 아이콘 → 파트너 선택 → 연락처/참고사항 입력 → 확인 모달(참고사항 미리보기 포함) → 발송까지 전 과정 정상 동작 확인. 공유 후 헤더의 "공유하기" 버튼이 변호사 프로젝트명 칩으로 즉시 전환되는 것도 확인(최근 커밋 `0431c3d` 반영 확인)
- [ ] **공유(일괄)**: 미확인
- [ ] **재공유**: 반려된 건이 없어 이번 세션엔 재현 못 함(미확인)
- [ ] **삭제 / 일괄 삭제**: 미확인(§2의 관련 이슈들은 코드 레벨에서 이미 수정 완료로 기록되어 있어 저위험 판단, 이번엔 스킵)

### B. 변호사(lawyer) 프로젝트

- [x] **목록**: 담당직원 컬럼(원 소유 프로젝트명) 노출 확인. 단, 이번 테스트 계정의 test02 목록엔 test01에서 공유된 건만 있고 자체생성 건이 없어 "함께 보이는지"는 간접 확인(별도 자체생성 케이스 미작성)
- [x] **검토 배너**: 검토중+공유중 건 상세 진입 시 "분석 데이터 검토 요청" 배너(반려/수락 버튼) 정상 노출
- [x] **수락**: 수락 사유 입력 → 정상 반영, 전달사항에 "수락" 항목+사유 표시됨. **한 가지 발견**: 공유 전 이미 결제 정보가 존재하던 건은 수락 즉시 진행절차 선택 모달이 뜨며 `계약대기중`을 건너뛰고 바로 `절차진행중`으로 전환됨(결제정보가 없는 건은 정상적으로 `계약대기중`에 머무름) — 버그는 아니고 의도된 분기로 보이나 문서화 안 돼 있던 동작이라 기록
- [ ] **거절**: 미확인(테스트 시간 관계상 스킵)
- [x] **공유받은 건 읽기 전용 확인**: 검토중 상태에서 상단 탭에 "상담 포인트" 탭 자체가 없음(AI 분석 추천/상담 포인트 숨김 확인). 절차 전환 비활성 배지는 검토중 상태에선 정적 텍스트로만 뜸(수락 후에는 정상 select로 전환되는 게 자연스러운 소유권 이전이라 판단)
- [ ] **자체 생성 건은 정상 편집 가능**: 미확인(test02에 자체생성 건 없음)

### C. 수임료(Fee Plan) — **문구 갱신함(2026-07-23)**: 계약대기중 "이전"에도 저장 가능하도록 이미 변경되어 있어([[project_fee_plan_early_save_flow_change]] 참고) 체크 항목을 실제 동작 기준으로 수정

- [x] **최초 입력 — 계약대기중 이전 단계**: `상담중` 상태에서 "결제정보" 버튼이 게이트 없이 클릭 가능했고, 저장 성공 + 절차선택 모달 **안 뜸**(의도대로)
- [x] **최초 입력 — 계약대기중 상태**: 계약대기중 건에서 결제정보 최초 저장 시 진행절차 선택 모달이 정상적으로 뜨고, 선택 후 상태가 즉시 `절차진행중 1/n`으로 전환됨
- [x] **회차 납부완료 처리 / 취소**: 체크 시 날짜 확인 UI → 완납 처리, 다시 클릭 시 "납부 처리 취소" 확인 모달 → 취소 시 미납으로 롤백. 진행률 바(0/12→1/12) 정상 갱신
- [x] **중단**: 사유 입력 후 중단 → 목록 상태가 `중도해지`로 반영, 전달사항에 "중단" 항목+사유 표시됨. 빨간 점 색상은 리스트뷰 텍스트로만 간접 확인(스크린샷 픽셀 대조는 못 함)
- [ ] **환불**: 미확인
- [ ] **통계 탭**: 미확인

### D. 전달사항(메시지 타임라인) — 상세 페이지 상단

- [x] 공유 시 항목 추가("공유") — 참고사항 텍스트까지 타임라인에 표시됨(§1 이슈2 수정 재확인 완료)
- [x] 수락 시 항목 추가("수락", 사유 메시지 표시) — 거절 케이스는 미확인
- [x] 수임료 최초입력 시 항목 추가("결제") — 금액/분할횟수/첫납부일 요약 표시 확인
- [x] 수임료 중단 시 항목 추가("중단") — 사유 표시 확인. 환불 케이스는 미확인
- [ ] 항목 3건 이상일 때 접기/펼치기: 3건 쌓인 케이스는 있었으나 접기/펼치기 인터랙션 자체는 미시도
- [x] §1 이슈 2 확인 완료: 참고사항이 공유 확인 모달 미리보기 + 상세 타임라인 양쪽에 정상 노출됨

### E. 회귀 확인 (이번 세션 변경분과 무관하지만 인접 코드)

- [ ] 고객 문자 전송 섹션 연락처 표시 타이포: 미확인
- [ ] 진단 목록 → 상세 → 목록 뒤로가기 시 필터/검색/페이지 상태 유지: 미확인

---

## 4. 모바일 — 별도 트랙 (데스크톱 확인 끝난 뒤)

기존 `docs/DEBT_RELIEF_MOBILE_RESPONSIVE_TASKS.md`의 Phase 진행 상황과 함께 아래 항목 추가 확인:

- [ ] `DiagnosisMobileCardList`에 공유 진입점이 없는 것이 의도된 범위인지 디자인 컨펌
- [ ] 이번 세션에 변경된 절차 전환/현재 단계 설정 게이팅이 모바일 상세 페이지(`SectionProcedureGuide`는 공용 컴포넌트)에서도 동일하게 동작하는지
- [ ] 재공유 플로우(프로젝트 선택 스킵)가 모바일 폭에서도 레이아웃 깨짐 없이 동작하는지

**2026-07-23 시도 결과**: `resize_window`로 390×844 뷰포트를 요청했으나 브라우저 자동화 툴의 스크린샷 캡처가 데스크톱 해상도로 그대로 나와 실제 모바일 뷰포트 재현에 실패함. 기존에 문서화된 "실기기/실뷰포트는 자동화로 재현 불가" 한계와 동일한 원인으로 판단되며, §4는 여전히 실기기·실브라우저 수동 확인이 필요.

---

## 5. 사람이 직접 눈으로 확인해야 하는 체크리스트 (자동화 QA로 대체 불가)

2026-07-23 브라우저 자동화 QA(§3·§4)에서 "에이전트가 계속 시도해볼 수 있는 것"과 "애초에 사람 눈/실기기가
아니면 확인 자체가 불가능한 것"을 나눴다. 이 섹션은 후자만 모은 것 — 아래 항목만 직접 확인하면 되고,
나머지(§3·§4의 남은 미확인 항목들)는 에이전트가 이어서 처리할 수 있으니 직접 돌아다니며 무작위로
확인할 필요 없음.

### 5-A. 실기기·픽셀 단위 육안 확인 (스크린샷 자동화로 재현 실패, 원인 확인됨)

- [ ] **모바일 375/390/430px 실기기 확인**: `docs/DEBT_RELIEF_MOBILE_RESPONSIVE_TASKS.md` §5 및 본 문서
      §4의 3개 항목 — `resize_window`가 실제 뷰포트를 재현하지 못해(스크린샷이 데스크톱 해상도로 캡처됨)
      자동화 불가로 확정됨. 실기기 또는 브라우저 개발자도구 반응형 모드로 직접 열어서 확인:
  - [ ] `DiagnosisMobileCardList` 공유 진입점 노출이 의도된 범위인지
  - [ ] 절차 전환/현재 단계 설정 게이팅이 모바일 상세(`SectionProcedureGuide`)에서도 데스크톱과 동일하게 동작하는지
  - [ ] 재공유 플로우(프로젝트 선택 스킵)가 모바일 폭에서 레이아웃 안 깨지는지
- [ ] **전달사항 타임라인 "중단"/"환불" 빨간 점 색상**: 기능(항목 추가, 사유 표시)은 확인됐으나 색상이
      실제로 반려와 동일한 빨간 점인지는 텍스트 기반 확인이라 픽셀로 못 봤음
- [ ] **고객 문자 전송 섹션 연락처 타이포**(semibold 14px)가 실제로 그렇게 렌더링되는지 육안 확인
- [ ] **라이트/다크 모드 전체 페이지 회귀**: `docs/DEBT_RELIEF_MOBILE_RESPONSIVE_TASKS.md` Phase 4에서
      실제 버그 1건이 나왔던 이력이 있어, 이번 세션 변경분(수임료/전달사항 UI)도 다크모드에서 색 대비·
      배경 뭉개짐 없는지 한 번 훑어볼 가치 있음

### 5-B. 실제 부수효과가 있어 자동화가 의도적으로 멈춘 것 (실행하면 진짜 발송/과금됨)

- [ ] **문자(SMS) 실제 발송 → 수신 확인**: 절차안내 단계별 문자 버튼, 하단 "고객 문자 전송" 섹션 4종
      (필요서류/상담일정/분석결과공유/직접작성) — 자동화는 발신번호·템플릿·예약 UI까지만 확인하고
      실제 "발송" 버튼은 안 눌렀음(실제 수신자에게 문자가 가고 과금되는 부수효과 때문). 본인 번호로
      테스트 발송해서 내용·발신번호·예약시각이 맞는지 실제 수신 확인 필요

---
