# 2026-08-19 분석 대상 채무 선택 수동 QA 체크리스트

작성일: 2026-08-19  
대상 범위: `df3a3a3..ceef389`  
실행: `npm run dev`

## 우선순위 요약

- 🔴 저장 대상과 분석 대상 분리: 제외한 채무가 삭제되지 않으면서 분석 계산에서는 빠져야 한다.
- 🔴 신규·수정 payload: 모든 채무가 전송되고 제외 항목에만 `isExcludedFromAnalysis: true`가 설정되어야 한다.
- 🟡 모달 반응형: 데스크톱 776px, 필수정보 모달 440px, 375px 모바일에서 가로 잘림이 없어야 한다.

## 0. 상태 조합 시나리오

### 시나리오 1 — 신규 분석 + 일부 채무 제외

재현: 채무를 2건 이상 입력 → 모든 필수정보 입력 → 분석하기 → 한 건 체크 해제 → 분석하기

왜 위험한가: `DiagnosisFormContent.handleDebtSelectionConfirm`이 동일한 채무 배열에 제외 플래그를 합성한다. 배열 자체를 필터링하면 저장 데이터가 유실된다.

- [x] 🔴 체크 해제한 채무를 포함해 입력한 모든 채무가 분석 상세/수정 화면에 남는다. (2026-08-19 브라우저 확인: 채무상세 모달에서 테스트은행A+테스트카드B 4,500,000원 전부 확인)
- [x] 🔴 체크 해제한 채무는 총채무·청산가치·면책액·AI 추천 계산에서 제외된다. (총채무 300만원으로 계산됨, AI 추천 문구에도 "총채무도 300만원" 명시)
- [ ] 🔴 요청의 `debts` 배열 건수는 입력 건수와 같고, 해제한 항목에만 `isExcludedFromAnalysis: true`가 들어간다. (코드 로직상 확인, 실제 요청 바디 raw 캡처는 못 함 — 결과가 정확히 일치해 간접 확인됨)

### 시나리오 2 — 기존 제외 상태 + 수정 재분석

재현: 일부 채무가 분석 제외된 기존 건 → 정보수정 → 분석하기

왜 위험한가: `AnalysisDebtSelectionModal`은 서버 응답의 `isExcludedFromAnalysis`를 초기 체크 상태로 복원하고, 수정 흐름은 선택 모달 뒤에 재분석 경고 모달을 추가로 거친다.

- [x] 🔴 기존에 제외된 채무만 체크 해제 상태로 복원된다. (2026-08-19 확인)
- [x] 🔴 선택 모달 확인 후 "다시 분석할까요?" 확인 모달이 순서대로 표시된다. (확인)
- [x] 🔴 재분석 취소 시 API가 호출되지 않고 선택한 제외 상태 때문에 기존 서버 데이터가 바뀌지 않는다. (네트워크 로그로 GET만 발생, PATCH/PUT 없음 확인)
- [x] 🔴 재분석 완료 후 제외 상태와 전체 채무 데이터가 새로고침 뒤에도 유지된다. (재분석 완료 → 페이지 새로고침 → 채무상세 모달·총채무 300만원 모두 유지 확인)

### 시나리오 3 — 분석 대상 0건 방지

재현: 선택 모달에서 모든 채무 체크 해제

왜 위험한가: 서버는 분석 포함 채무를 최소 1건 요구한다.

- [ ] 🔴 분석하기 버튼이 비활성화되어 빈 분석 요청이 전송되지 않는다.
- [ ] 다시 한 건을 체크하면 분석하기 버튼이 즉시 활성화된다.

## A. 모달 UI와 반응형

- [x] 🔴 **데스크톱에서 채무 현황 선택 모달의 레이아웃 너비가 776px이다.** (2026-08-24 재측정: computed width 775.996px, 렌더 620.797px(zoom 0.8) — 스펙과 일치. 아래 "발견된 이슈 3" 갱신 참고.)
- [ ] 🟡 채무가 많을 때 목록만 세로 스크롤되고 헤더와 하단 버튼은 유지된다. (미확인)
- [ ] 🟡 375px에서 헤더·금액·버튼이 화면 밖으로 잘리지 않는다. (미확인 — 브라우저 자동화로 뷰포트 리사이즈가 안 먹혀 실측 불가, **사람이 직접 확인 필요**)
- [x] 🟡 필수정보 누락 모달은 440px 이상 화면에서 440px를 유지한다. (2026-08-19 확인: computed width 440px, 렌더 352px = 440×zoom0.8 정확히 일치) 375px에서 16px 여백으로 축소되는지는 미확인(위와 동일 사유)
- [ ] 🌗 라이트/다크 모드에서 체크 상태, 행 배경, 닫기 아이콘, 분석하기 버튼의 대비가 유지된다. (미확인, **사람이 직접 확인 필요**)
- [ ] 데스크톱 폭 1080px 이상 UI zoom 0.8 환경에서 776px 모달이 중앙 정렬되고 금액 열이 잘리지 않는다. (zoom 0.8 자체는 활성 상태에서 테스트했으나 위 776px 이슈로 인해 의도한 폭이 아님 — 육안 확인은 **사람이 직접 필요**)

## B. 회귀 확인

- [x] 필수정보 누락 시 채무 선택 모달보다 필수정보 안내 모달이 먼저 표시된다. (2026-08-19 확인: 자산현황 미입력 상태에서 분석하기 클릭 시 필수정보 모달이 채무선택모달보다 먼저 뜸)
- [ ] 유효하지 않은 채무 금액·필수 채무 필드는 기존 오류 안내 후 채무 단계로 이동한다.
- [ ] 모든 채무를 체크한 경우 기존 분석 결과와 계산값이 동일하다.
- [ ] 분석 요청 실패 시 서버 메시지·에러 코드가 UI에 노출되지 않고 재시도 가능한 상태로 복구된다.

## 발견된 이슈 (2026-08-19 브라우저 QA)

1. **(0순위, 비즈니스 로직 결함 — 재현·수정 완료)** 간편 모드로는 새 진단을 분석 자체를 못 하던 버그.
   `useDiagnosisForm.ts`의 `derived.totalDebtManwon`이 간편 모드에서 레거시 필드 `form.debtTypes`/`form.debtAmounts`를
   합산했는데, 현재 채무 입력 UI(`DebtItemsTable`)는 간편·상세 모드 모두 `form.debts`에만 기록해서 저 두 필드는
   항상 비어있음 — 즉 간편 모드는 채무를 아무리 입력해도 총채무가 0원으로 계산되고, `DiagnosisFormContent.tsx:310`의
   `totalDebtManwon <= 0` 가드에 걸려 "분석하기"가 100% 차단됨. `form.debts` 기준으로 통일해서 수정, 간편 모드로
   실제 분석 성공(201, 결과 페이지 정상 렌더)까지 확인.
2. **(로컬 환경 설정, 수정 완료)** `.env.local`의 `NEXT_PUBLIC_SITE_URL`이 배포 도메인(`app-dev.talkgate.im`)을
   가리키고 있어서, 로컬 `npm run dev`에서 서브도메인 프로젝트(`test01.localhost:3000`)로 접근해도 미들웨어가
   원격 배포 환경으로 리다이렉트시킴. `http://localhost:3000`으로 수정.
3. **(해결됨, 2026-08-24 재측정 확인)** 채무 현황 선택 모달(776px)의 실제 렌더 폭이 스펙과 다르던 문제.
   08-19 당시: `computed width: 697.559px`(776px 아님), 렌더 폭 558px(zoom 0.8 적용 시 기대값 620.8px 아님).
   근본 원인은 끝내 못 밝혔으나, 이후 진단서 폼 전체의 데스크톱 전환 기준을 `md:`(780px)에서 `lg:`(1080px)로
   바꾼 작업(커밋 `7b8c39b`, `AnalysisDebtSelectionModal.tsx` 포함 15개 파일)이 결과적으로 이 문제도 해소함.
   2026-08-24 `테스트프로젝트test01`(데스크톱 1424px, zoom 0.8)에서 재측정: `computed width 775.996px`(스펙
   776px과 일치), 렌더 폭 620.797px(기대값 620.8px과 일치). 시각적으로도 중앙정렬·잘림 없음 확인. 원인이
   `md:` variant 자체의 컴파일 문제였는지, 다른 요인이었는지는 여전히 불명확 — 재발 시 이 커밋을 우선
   의심할 것.

4. **(0순위, 비즈니스 로직 결함 — 재현·수정 완료, 2026-08-19)** 상세모드 채무 항목의 대출일·만기일·금리를
   한 번도 입력하지 않아도 좌측 네비게이터 "3. 채무현황"에 완료 체크(v)가 표시되던 버그.
   재현: `/debt-relief/{id}/edit?step=3`에서 간편모드로 입력해둔 채무를 상세 탭으로 전환(대출일/만기일/금리
   미입력) → 네비게이터 3번에 v 표시. 원인: `FormStepChecklist.tsx`의 체크(v) 판정이 쓰는
   `isDiagnosisStepComplete`가 `getMissingRequiredFieldLabelsForStep("debts")`를 그대로 재사용하는데, 그
   함수가 부르는 `getMissingDebtFieldLabels`는 "분석하기" 버튼이 새 빈 행 때문에 영구 비활성화되는 걸 막으려고
   대출일·만기일·금리를 의도적으로 검사 안 함(코드 주석 참고) — 그런데 같은 함수가 체크리스트에도 쓰이면서
   간편→상세 전환 시 채권처·현재잔액만 채워진 상태에서도 완료로 오판정됨. `isDiagnosisStepComplete`에서 debts
   스텝일 때만 `getMissingDebtItemFieldLabels`(상세모드 전용, 대출일/만기일/금리)도 함께 검사하도록 수정.
   "다음"/"이전" 버튼 게이트(`getMissingRequiredFieldLabelsForStep` 직접 호출부)와 "분석하기"
   버튼(`canAnalyze`)은 사용자 요청대로 그대로 두고, 체크리스트 표시만 정확해지도록 스코프를 좁힘 —
   미완성 상태에서도 스텝 이동은 계속 가능하고, 최종 검증은 기존대로 분석하기 클릭 시 이루어짐. `npx tsc
   --noEmit` 통과 확인, 브라우저 재현 검증은 미수행.

5. **(1순위, UI/네트워크 비효율 — 재현·원인 확인·수정 완료, 2026-08-20)** 분석 상세 페이지 진입 시
   `GET /v1/analysis/{id}/chat`이 중복 호출됨(브라우저 자동화 재현 시 dev 기준 4회).
   `SectionCounselMents.tsx`와 상시 마운트되는(다운로드 버튼 클릭 여부와 무관) 인쇄용 숨김
   문서 `AnalysisPrintDocument.tsx`가 각자 독립적으로 `useDebtReliefAiChat(detail.id,
   projectId)`를 호출해서 같은 히스토리를 두 번 fetch함 — 이건 프로덕션에서도 그대로 재현됨(2회).
   dev 배수(최대 4회)는 `next.config.ts`에 `reactStrictMode` 미설정 시 App Router가 기본 활성화하는
   Strict Mode의 effect 더블 실행 때문(dev 전용, 빌드엔 영향 없음, 미수정).
   수정: `useDebtReliefAiChat.ts`의 히스토리 조회 책임을 새 훅 `useDebtReliefChatHistory`로 분리해
   `ResultDetailContent`에서 한 번만 호출하고, 결과(`DebtReliefChatHistory`)를 `SectionCounselMents`에는
   `chatHistory` prop으로, `AnalysisPrintDocument`에는 이미 조회된 `chatMessages` 배열로 내려줌.
   `useDebtReliefAiChat`은 `history.loaded`가 처음 true가 될 때 한 번만 로컬 상태에 반영하도록 해
   전송 중/스트리밍 중인 메시지가 덮어써지지 않게 함. `npx tsc --noEmit` / `npx eslint` 통과 확인,
   브라우저 재현 검증은 미수행.

## 종료 기준

- 🔴 항목 전부 통과
- 신규와 수정 각각 payload 및 새로고침 후 데이터 유지 확인
- 375px 모바일과 데스크톱 UI zoom 0.8에서 모달 육안 확인
