# 인증 시스템 서버 사이드 마이그레이션 완료 요약

## ✅ 완료된 작업

### 1. API 프록시 시스템 구축
- **파일**: `src/app/api/proxy/[...path]/route.ts`
- **기능**: 모든 백엔드 API 호출을 서버 프록시를 통해 처리
- **특징**:
  - httpOnly 쿠키에서 토큰 자동 읽기
  - 401 응답 시 자동 토큰 갱신
  - 쿠키 자동 전달

### 2. 로그인 API 서버 사이드로 변경
- **파일**: `src/app/api/auth/login/route.ts`
- **변경사항**:
  - 서버에서 httpOnly 쿠키 설정
  - 클라이언트에서 토큰 관리 제거

### 3. 소셜 로그인 API 서버 사이드로 변경
- **파일**: `src/app/api/auth/social/[provider]/route.ts`
- **변경사항**:
  - Google, Kakao, Naver 로그인 모두 서버에서 처리
  - 서버에서 httpOnly 쿠키 설정

### 4. 로그아웃 API 서버 사이드로 변경
- **파일**: `src/app/logout/route.ts`
- **변경사항**:
  - 서버에서 httpOnly 쿠키 삭제
  - 랜딩 페이지 콜백 지원

### 5. ApiClient 수정
- **파일**: `src/lib/apiClient.ts`
- **변경사항**:
  - 백엔드 직접 호출 → 프록시를 통한 호출로 변경
  - 클라이언트에서 쿠키 읽기 제거
  - 토큰 갱신 로직 제거 (프록시에서 자동 처리)
  - credentials: 'include' 설정

### 6. AuthService 수정
- **파일**: `src/services/auth.ts`
- **변경사항**:
  - 로그인: 서버 API 라우트 사용
  - 소셜 로그인: 서버 API 라우트 사용
  - setTokens 호출 제거
  - 토큰은 서버에서 자동 관리

### 7. Header 컴포넌트 수정
- **파일**: `src/components/layout/Header.tsx`
- **변경사항**:
  - 로그아웃 버튼: 서버 API 호출로 변경
  - clearTokens 제거

## 🔄 변경된 플로우

### 로그인 플로우
```
이전: 클라이언트 → 백엔드 API → 클라이언트에서 쿠키 설정
현재: 클라이언트 → 서버 API → 서버에서 httpOnly 쿠키 설정
```

### API 호출 플로우
```
이전: 클라이언트 → 백엔드 API (클라이언트에서 Authorization 헤더 추가)
현재: 클라이언트 → 프록시 API → 백엔드 API (서버에서 쿠키 읽어서 헤더 추가)
```

### 로그아웃 플로우
```
이전: 클라이언트에서 쿠키 삭제
현재: 클라이언트 → 서버 로그아웃 API → 서버에서 쿠키 삭제
```

## 🔒 보안 개선

1. **httpOnly 쿠키**: XSS 공격으로부터 토큰 보호
2. **서버 사이드 토큰 관리**: 클라이언트에서 토큰 접근 불가
3. **자동 토큰 갱신**: 프록시에서 401 응답 시 자동 처리

## 📝 남은 작업

### 랜딩 페이지 업데이트
- **파일**: `LANDING_PAGE_AUTH_UPDATE_PROMPT.md` 참조
- **내용**: 랜딩 페이지의 인증 상태 확인 로직 수정 필요

### 테스트 필요 항목
1. 로그인 플로우 테스트
2. 소셜 로그인 플로우 테스트
3. 로그아웃 플로우 테스트
4. 토큰 갱신 자동 처리 테스트
5. API 호출 프록시 동작 테스트
6. 랜딩 페이지 연동 테스트

## 🐛 알려진 제약사항

1. **FormData 처리**: 프록시에서 FormData를 Blob으로 변환하여 처리하고 있음. 필요시 개선 필요
2. **파일 업로드**: 큰 파일 업로드 시 프록시를 통한 전달이 성능에 영향을 줄 수 있음

## 📚 참고 문서

- 랜딩 페이지 업데이트 프롬프트: `LANDING_PAGE_AUTH_UPDATE_PROMPT.md`

